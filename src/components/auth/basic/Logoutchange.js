import React from 'react';
import LogoutContentChangePassword from '../LogoutContentChangePassword.';

const Logout = () => (
  <div className="text-center">
    <LogoutContentChangePassword />
  </div>
);

export default Logout;
